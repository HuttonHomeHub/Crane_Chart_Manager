# Dev / test / CI-quality tooling. NOT installed into the runtime image — keeping
# these out of requirements.txt shrinks the image and its dependency attack surface
# (SEC-DEP-02). Install locally with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-playwright>=0.6.0
pytest-cov>=5.0          # coverage gate (REM-OPS-05 / T3)
ruff>=0.6                # lint + format (REM-OPS-01a)
pip-audit>=2.7           # dependency CVE scan (REM-SEC-02 / S2)
