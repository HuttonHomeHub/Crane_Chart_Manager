__pycache__/
.venv/
.pytest_cache/

# Runtime state — user's catalogue lives here; the app re-creates an empty
# uploads/ directory on startup if missing. Commit individual files if you
# want them tracked.
uploads/
metadata.json
metadata.json.lock
metadata.json.tmp
metadata.json.migrated

# R-019: SQLite metadata store (replaces metadata.json above) and its WAL sidecars.
crane.db
crane.db-wal
crane.db-shm
