# Runtime dependencies only — this is what the Docker image installs.
# SEC-DEP-01: Flask 3.1.1 → 3.1.3 fixes CVE-2026-27205 (surfaced by pip-audit in CI).
Flask==3.1.3
Flask-Limiter==4.1.1
gunicorn==26.0.0
python-json-logger==4.1.0
